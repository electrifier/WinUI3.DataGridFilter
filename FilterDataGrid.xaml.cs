using Microsoft.UI.Xaml;
using Microsoft.UI.Xaml.Controls;

namespace WinUI3.DataGridFilter
{
    public sealed partial class FilterDataGrid : Page
    {
        public FilterDataGrid()
        {
            this.InitializeComponent();
        }

        // ...existing code...
    }
}
